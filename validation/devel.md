# Validation development guide

The canonical developer guide is [`DEVEL.md`](DEVEL.md). It explains how to add cases, register future suites, expose them through workflows, the root CLI and UI, preserve evaluator-only marking boundaries, package release assets, and add regression coverage.