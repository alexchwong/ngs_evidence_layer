__pycache__/
*.pyc
.venv/
venv/

# Working files from a retrieval session. The rendered block is the deliverable;
# the intermediate JSON is not worth keeping.
step2.json
bundle.json
block.md

# Built corpus and per-publication outputs are regenerable from input/ plus the
# ingestion sessions. Commit them only deliberately, when sealing a version.
output/corpus/
output/reports/build-report.json
