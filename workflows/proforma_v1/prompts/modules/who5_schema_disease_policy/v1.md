## WHO5 schema-disease routing policy

The following disease labels are excluded from authoritative WHO5 `schema_disease` routing. This policy is enforced deterministically by the workflow.

- `MDS/AML`
