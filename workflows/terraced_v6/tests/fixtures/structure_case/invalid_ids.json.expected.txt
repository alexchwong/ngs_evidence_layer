structured case failed validation with 4 issue(s):
1. variants[0].variant_id
   Problem: received 'v01'.
   Required fix: use sequential stable ID V1.
2. variants[0].gene
   Problem: invalid gene 'srsf2'.
   Required fix: use uppercase reported gene symbol.
3. variants[0].description
   Problem: description does not begin with gene 'srsf2'.
   Required fix: prefix the unchanged detailed variant description with exact gene 'srsf2'.
   Received: 'p.P95H'
   Expected: srsf2 + complete reported variant description
4. case_facts[0].fact_id
   Problem: received 'F1'.
   Required fix: use sequential C1.
