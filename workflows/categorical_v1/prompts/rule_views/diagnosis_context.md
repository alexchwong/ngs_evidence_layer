## Established diagnostic context

The Step-3 CMC was unchanged. The following is the validated integrated diagnosis summary from `report-summary-dx.yaml`; use it as established patient-level diagnostic context when answering R2-R5.

This is context only, not a literature-evidence source. Do not copy citation tags from it unless the same runtime tag is independently exposed in `downstream_evidence.md`.

```yaml
{{REPORT_SUMMARY_DX}}
```
