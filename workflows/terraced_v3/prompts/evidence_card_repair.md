# Local card-pairing repair

The facts below are frozen. Do not rewrite them.
For each fact, select the local CARD labels whose interpretations reasonably support the fact.
Use only labels from the supplied candidate card blocks. Use `card_refs: []` when no literature card is appropriate.
Do not invent labels.

Return YAML only:

```yaml
repairs:
  - candidate_id: C1
    card_refs: ["CARD 01"]
```

Return exactly one repair row for every supplied candidate_id.

# Frozen facts
{{facts}}

# Candidate card blocks
{{cards}}
