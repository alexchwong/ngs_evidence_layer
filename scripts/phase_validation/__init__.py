"""Phase-specific deterministic validators.

Each phase module used in an LLM prompt is self-contained and has no repository-file
dependencies. ``scripts/final_validation.py`` is a compatibility facade over these
modules.
"""
